
modLoader="javafml" #mandatory
loaderVersion="[36,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
issueTrackerURL="https://github.com/Aspurgus/Aspr-And-Alliums/issues"
license="MIT License"

[[mods]] #mandatory
modId="asprandalliums" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Aspr and Alliums" #mandatory
displayURL="https://github.com/Aspurgus/Aspr-And-Alliums"
logoFile="logo.png" #optional
logoBlur= false
credits="The free time of no school" #optional
authors="Aspr and Aspr" #optional
description='''
From a little cottage of friends with some cheescake downstairs
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.asprandalliums]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[36,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.asprandalliums]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.16.5,1.17)"
    ordering="AFTER"
    side="BOTH"
[[dependencies.asprandalliums]]
    modId = "abnormals_core"
    mandatory = true
    versionRange = "[3.2.0,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.asprandalliums]]
    modId = "farmersdelight"
    mandatory = true
    versionRange = "[0.5.4,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.asprandalliums]]
    modId = "nethers_delight"
    mandatory = true
    versionRange = "[2.1,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.asprandalliums]]
    modId = "savageandravage"
    mandatory = true
    versionRange = "[3.1,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.asprandalliums]]
    modId = "endergetic"
    mandatory = true
    versionRange = "[3.0.0,)"
    ordering = "AFTER"
    side = "BOTH"